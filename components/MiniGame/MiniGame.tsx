import React from "react";
import { motion } from "framer-motion";

export const MiniGame = () => {
  return (
    <motion.section>
      <motion.div>Play</motion.div>
      <motion.div>The Game</motion.div>
    </motion.section>
  );
};
